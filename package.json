{
  "name": "mapa-escolas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync": "node scripts/sync_escolas.mjs",
    "deploy": "npm run build && npx vercel deploy --prod"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  },
  "dependencies": {
    "jspdf": "^4.2.1"
  }
}
